{
    "name": "kval",
    "version": "0.0.1",
    "description": "",
    "scripts": {
        "dev": "pnpm run --filter=./apps/* dev",
        "dev:frontend": "pnpm run --dir apps/frontend dev",
        "dev:backend": "pnpm run --dir apps/backend dev",
        "preinstall": "npx only-allow pnpm",
        "format": "prettier . --write",
        "build:libs": "pnpm run --filter=./libs/* build",
        "preinstall": "npx only-allow pnpm",
        "format": "prettier . --write"
    },
    "license": "ISC",
    "private": true,
    "type": "module",
    "packageManager": "pnpm@9.6.11",
    "engines": {
        "pnpm": "^9.0.0"
    },
    "devDependencies": {
        "@nestjs/cli": "^10.4.9",
        "prettier": "^3.0.0"
    }
}
